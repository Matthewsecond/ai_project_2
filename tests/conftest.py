"""Pytest configuration. The package is installed editable (`pip install -e .`),
so tests import it directly: `from jobs_intelligence_ai.search.orchestrator import Orchestrator`.
Shared fixtures go here as the suite grows."""
